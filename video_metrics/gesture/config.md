# Пороговые значения и обоснование метрики жестикуляции

## Выбранный алгоритм

**Алгоритм:** `TBD_после_обсуждения`

Здесь нужно описать алгоритм, который был выбран после сравнения всех кандидатов.

## Почему был выбран именно этот алгоритм

Нужно зафиксировать:
- как он показал себя относительно baseline из Charisma Master,
- насколько хорошо он разделил `good_professional` и `bad`,
- оказались ли `self_recorded` ближе к `good_professional`, чем к `bad`,
- насколько стабильно он работал на `near`, `mid` и `far`,
- устроило ли время работы на CPU.

## Определение метрики

Здесь нужно описать формулу сырой метрики.

Рекомендуемая структура описания:
- какие keypoints используются на входе,
- какая применяется предобработка,
- какие признаки считаются на кадр,
- как они агрегируются во времени,
- как получается итоговый raw score.

## Пороги

- `gesture_threshold_bad`: `TBD`
- `gesture_threshold_good`: `TBD`
- `gesture_distance_stability_tolerance`: `TBD`

Нужно объяснить:
- какие значения соответствуют слабой жестикуляции,
- какой диапазон соответствует нормальной / сильной профессиональной жестикуляции,
- какое расхождение между одинаковыми роликами на разном расстоянии от камеры ещё считается допустимым.

## Коэффициенты и веса

Если в финальном алгоритме используются веса, их нужно задокументировать здесь.

- `gesture_weight_activity`: `TBD`
- `gesture_weight_amplitude`: `TBD`
- `gesture_weight_rhythm`: `TBD`
- `gesture_weight_variability`: `TBD`
- `gesture_weight_pointing_bonus`: `TBD`

Для каждого коэффициента нужно описать:
- какой именно сигнал он усиливает,
- зачем это полезно,
- какой компромисс это создаёт.

## Обратная связь 

Здесь нужно зафиксировать фидбэк, который повлиял на итоговые пороги.

Удобный шаблон:
- дата,
- краткое содержание комментария,
- что именно было изменено после обсуждения.

## Примечание про baseline

Текущий baseline в Charisma Master использует `gesture_score`, который считается из среднего движения рук, масштабируется коэффициентом из конфига и затем сравнивается с нижним и верхним порогом.

Референсная реализация:
- `charisma-master/services/ml_worker/app/logic/ml_engine.py`
- `charisma-master/services/ml_worker/app/config.json`
- `charisma-master/services/ml_worker/app/config_fields.md`
